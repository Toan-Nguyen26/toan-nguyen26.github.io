# Hình cho `slides/introduction.html`

Drop the real figures in here with these exact filenames — the deck picks them up
automatically. Until a file exists, the slide shows a dashed placeholder naming it.

| File | Slide |
|---|---|
| `rag-framework.png` | 1 · Hierarchical text segmentation chunking — overall framework |
| `diffusion-framework.png` | 2 · Latent diffusion segmentation — framework |
| `diffusion-examples.png` | 2 · Qualitative results — predictions vs. ground truth |
| `uncertainty-3d-result.png` | 3 · Calibrated uncertainty — 3D volume + uncertainty map |
| `pathology-moe.png` | 4 · Pathology-routed LoRA experts — router + experts |

Any web format works (`.png`, `.jpg`, `.svg`); if you use a different extension,
update both `src` and `data-src` on that `<figure class="img-slot">`.
